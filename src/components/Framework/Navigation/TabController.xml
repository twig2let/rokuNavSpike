<?xml version="1.0" encoding="UTF-8"?>
<component name="TabController" extends="BaseAggregateView">
    <script type="text/brightscript" uri="TabController.brs" />

    <interface>
        <!-- nav item of the currently displayed view -->
        <field id="currentItem" type="node"/>
        
        <!-- If you have a preexisting view that needs to be associated with a tab 
        item, then create the view and register it here, before setting the menuItems -->
        <function name="addExistingView"/>
        <function name="getViewForMenuItemContent"/>
        <function name="getViews"/>

        <function name="changeCurrentItem"/>
    </interface>
</component>
